# Your code below:

single_digits = list(range(10))
#print(single_digits)

squares = []
for i in single_digits:
  squares.append(i**2)
  print(i)
print(squares)

cubes = [i**3 for i in single_digits]
print(cubes)
